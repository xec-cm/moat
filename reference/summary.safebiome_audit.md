# Print a safebiome audit summary

Print a safebiome audit summary

## Usage

``` r
# S3 method for class 'safebiome_audit'
summary(object, verbose = FALSE, ...)
```

## Arguments

- object:

  A `safebiome_audit` object.

- verbose:

  A single logical value. When `TRUE`, include all module reasons and
  recommendations. Defaults to `FALSE`.

- ...:

  Additional arguments passed to methods.

## Value

A `summary.safebiome_audit` object.
