% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-ordination.R
\name{plot_ordination}
\alias{plot_ordination}
\title{Plot PCoA ordination coordinates from a batch audit}
\usage{
plot_ordination(audit, color = NULL, distance = NULL)
}
\arguments{
\item{audit}{A \code{safebiome_audit} object.}

\item{color}{Optional single string naming a metadata variable. When \code{NULL},
the first batch variable is used when available, otherwise the outcome.}

\item{distance}{Optional character vector naming audited distances to plot.
When \code{NULL}, the first available distance is used. Use \code{"all"} to plot all
audited distances.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
\code{plot_ordination()} visualizes the first two PCoA axes stored by
\code{\link[=check_batch]{check_batch()}}. Points can be colored by any audited outcome, batch, or
covariate variable stored with the ordination coordinates.
}
\examples{
data("toy_biome")
audit <- check_biome(toy_biome, outcome = "outcome", batch = "batch", n_perm = 99)
plot_ordination(audit, color = "batch", distance = "bray")
}
