% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-correction.R
\name{check_balance}
\alias{check_balance}
\title{Check batch-by-outcome balance}
\usage{
check_balance(metadata, outcome, batch)
}
\arguments{
\item{metadata}{A data frame with sample metadata.}

\item{outcome}{A single string naming the outcome variable in \code{metadata}.}

\item{batch}{A character vector naming batch variables in \code{metadata}.}
}
\value{
A data frame with one row per batch variable.
}
\description{
\code{check_balance()} audits whether each batch level contains enough outcome
support for adjustment to be statistically plausible.
}
\examples{
metadata <- data.frame(
  condition = rep(c("control", "case"), each = 6),
  center = rep(c("A", "B"), times = 6)
)

check_balance(metadata, outcome = "condition", batch = "center")
}
