% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-batch.R
\name{check_feature_batch}
\alias{check_feature_batch}
\title{Check feature-level batch associations}
\usage{
check_feature_batch(
  x,
  metadata = NULL,
  batch = NULL,
  outcome = NULL,
  assay = "counts",
  transform = "relative",
  p_adjust_method = "BH",
  alpha = 0.05,
  effect_size_threshold = 0.1
)
}
\arguments{
\item{x}{A numeric matrix-like object or a
\code{\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment()}} object.}

\item{metadata}{Optional data frame with sample metadata. When \code{x} is a
\code{SummarizedExperiment}, \code{colData(x)} is used by default.}

\item{batch}{Optional character vector naming batch variables in \code{metadata}.}

\item{outcome}{Optional single string naming the outcome variable in
\code{metadata}. When supplied, outcome associations are reported as context.}

\item{assay}{A single string naming the assay to extract when \code{x} is a
\code{SummarizedExperiment}. Defaults to \code{"counts"}.}

\item{transform}{A single string naming the feature transformation. Supported
values are \code{"relative"}, \code{"clr"}, \code{"presence_absence"}, and \code{"none"}.
Defaults to \code{"relative"}.}

\item{p_adjust_method}{A single string naming the p-value adjustment method
passed to \code{\link[stats:p.adjust]{stats::p.adjust()}}. Defaults to \code{"BH"}.}

\item{alpha}{A single number in \verb{(0, 1]} used as the adjusted p-value
threshold. Defaults to \code{0.05}.}

\item{effect_size_threshold}{A single number in \verb{[0, 1]} used as the minimum
R2 for a moderate feature-level batch signal. Defaults to \code{0.10}.}
}
\value{
A list with feature-level batch diagnostics and recommendations.
}
\description{
\code{check_feature_batch()} screens individual microbiome features for strong
association with batch variables. The diagnostic is intended as a
pre-analysis audit signal, not as a replacement for differential abundance
modelling.
}
\examples{
data("toy_moat")
check_feature_batch(toy_moat, batch = "batch", outcome = "outcome")
}
