% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-variance.R
\name{plot_variance}
\alias{plot_variance}
\title{Plot PERMANOVA variance explained by audited terms}
\usage{
plot_variance(audit, distance = NULL)
}
\arguments{
\item{audit}{A \code{moat_audit} object.}

\item{distance}{Optional character vector naming audited distances to plot.
When \code{NULL}, the first available distance is used. Use \code{"all"} to plot all
audited distances.}
}
\value{
A \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
\code{plot_variance()} visualizes the PERMANOVA R2 terms stored by the batch
audit. Outcome, batch, covariate, and other terms are colored separately so
batch dominance is easy to inspect.
}
\examples{
data("toy_moat")
audit <- moat(toy_moat, outcome = "outcome", batch = "batch", n_perm = 99)
plot_variance(audit, distance = "bray")
}
