% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-metadata-predictability.R
\name{check_metadata_predictability}
\alias{check_metadata_predictability}
\title{Check whether metadata alone predicts the outcome}
\usage{
check_metadata_predictability(
  metadata,
  outcome,
  predictors,
  n_folds = 5,
  seed = 1
)
}
\arguments{
\item{metadata}{A data frame with sample metadata.}

\item{outcome}{A single string naming a binary outcome variable in
\code{metadata}.}

\item{predictors}{A character vector naming metadata variables used as
predictors.}

\item{n_folds}{A single positive integer giving the requested number of
cross-validation folds. The effective number of folds is reduced when the
smallest outcome class is smaller than \code{n_folds}.}

\item{seed}{A single integer seed used to make fold assignment reproducible.}
}
\value{
A list with model formula, predictors used, dropped predictors,
cross-validated balanced accuracy, apparent balanced accuracy, risk,
warnings, and recommendations.
}
\description{
\code{check_metadata_predictability()} fits a simple metadata-only logistic model
for binary outcomes and evaluates it with stratified cross-validation. High
predictability suggests design confounding or validation leakage risk before
microbiome features are used.
}
\examples{
metadata <- data.frame(
  outcome = rep(c("control", "case"), each = 8),
  center = rep(c("A", "B"), each = 8)
)

check_metadata_predictability(
  metadata,
  outcome = "outcome",
  predictors = "center",
  n_folds = 4
)
}
