% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-correction.R
\name{check_correction}
\alias{check_correction}
\title{Check batch correction feasibility}
\usage{
check_correction(metadata, outcome, batch = NULL, covariates = NULL)
}
\arguments{
\item{metadata}{A data frame with sample metadata.}

\item{outcome}{A single string naming the outcome variable in \code{metadata}.}

\item{batch}{Optional character vector naming batch variables.}

\item{covariates}{Optional character vector naming covariates.}
}
\value{
A list with correction feasibility diagnostics, including balance
diagnostics, model matrix diagnostics, a positivity score, a feasibility
category, and recommendation text.
}
\description{
\code{check_correction()} combines batch-by-outcome balance and model matrix
identifiability diagnostics to determine whether batch adjustment is
statistically feasible.
}
\examples{
metadata <- data.frame(
  condition = rep(c("control", "case"), each = 6),
  center = rep(c("A", "B"), times = 6),
  age = seq(30, 41)
)

check_correction(metadata, outcome = "condition", batch = "center", covariates = "age")
}
