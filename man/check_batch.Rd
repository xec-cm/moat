% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-batch.R
\name{check_batch}
\alias{check_batch}
\title{Check microbiome batch effects across distances}
\usage{
check_batch(
  x,
  metadata = NULL,
  outcome,
  batch = NULL,
  covariates = NULL,
  assay = "counts",
  transform = "auto",
  distances = c("aitchison", "bray"),
  n_perm = 999,
  order_sensitivity = TRUE
)
}
\arguments{
\item{x}{A numeric matrix-like object or a
\code{\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment()}} object.}

\item{metadata}{Optional data frame with sample metadata. When \code{x} is a
\code{SummarizedExperiment}, \code{colData(x)} is used by default.}

\item{outcome}{A single string naming the outcome variable in \code{metadata}.}

\item{batch}{Optional character vector naming batch variables in \code{metadata}.}

\item{covariates}{Optional character vector naming covariates in \code{metadata}.}

\item{assay}{A single string naming the assay to extract when \code{x} is a
\code{SummarizedExperiment}. Defaults to \code{"counts"}.}

\item{transform}{A single string naming the microbiome transformation to use
before distance calculation. Use \code{"auto"} to choose the default
transformation for each distance. Defaults to \code{"auto"}.}

\item{distances}{A character vector naming microbiome distances. Supported
values are those accepted by \code{\link[=compute_biome_distance]{compute_biome_distance()}}.}

\item{n_perm}{A single positive integer giving the number of permutations.}

\item{order_sensitivity}{A single logical value indicating whether to compare
outcome-first and batch-first PERMANOVA term orders. Defaults to \code{TRUE}.}
}
\value{
A list with batch audit diagnostics and recommendations.
}
\description{
\code{check_batch()} combines distance calculation, PERMANOVA, dispersion, and
PCoA diagnostics into a batch-risk summary. Full dispersion diagnostics are
stored in \code{dispersion}; \code{permdisp} remains as a batch-only compatibility
table. PCoA diagnostics include coordinates, variance explained, and
axis-by-metadata association tests.
}
\examples{
data("toy_moat")
check_batch(toy_moat, outcome = "outcome", batch = "batch", n_perm = 99)
}
