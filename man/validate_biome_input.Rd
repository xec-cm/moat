% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input-validation.R
\name{validate_biome_input}
\alias{validate_biome_input}
\title{Validate microbiome audit input}
\usage{
validate_biome_input(
  se,
  outcome,
  batch = NULL,
  covariates = NULL,
  subject = NULL,
  time = NULL,
  assay = "counts"
)
}
\arguments{
\item{se}{A \code{\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment()}} object. Objects
extending \code{SummarizedExperiment}, such as \code{TreeSummarizedExperiment}, are
accepted.}

\item{outcome}{A single string naming the outcome variable in \code{colData(se)}.}

\item{batch}{Optional character vector naming batch variables in
\code{colData(se)}.}

\item{covariates}{Optional character vector naming covariates in
\code{colData(se)}.}

\item{subject}{Optional single string naming the subject identifier variable
in \code{colData(se)}.}

\item{time}{Optional single string naming the time variable in \code{colData(se)}.}

\item{assay}{A single string naming the assay to audit. Defaults to
\code{"counts"}.}
}
\value{
A list with clean sample metadata, an assay summary, and the resolved
variable names.
}
\description{
Checks that the input object and metadata variables satisfy the minimum
requirements for running a \code{safebiome} audit. The function is intentionally
internal: exported user-facing functions should call it before dispatching
to design, batch, correction, or leakage modules.
}
\keyword{internal}
