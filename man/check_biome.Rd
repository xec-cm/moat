% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-biome.R
\name{check_biome}
\alias{check_biome}
\title{Audit a microbiome study design}
\usage{
check_biome(
  x,
  outcome,
  batch = NULL,
  covariates = NULL,
  subject = NULL,
  time = NULL,
  assay = "counts",
  transform = "clr",
  distances = c("aitchison", "bray"),
  n_perm = 999,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{\link[SummarizedExperiment:SummarizedExperiment-class]{SummarizedExperiment::SummarizedExperiment()}} object. Objects
extending \code{SummarizedExperiment}, such as \code{TreeSummarizedExperiment}, are
accepted.}

\item{outcome}{A single string naming the outcome variable in \code{colData(x)}.}

\item{batch}{Optional character vector naming batch variables in
\code{colData(x)}.}

\item{covariates}{Optional character vector naming covariates in
\code{colData(x)}.}

\item{subject}{Optional single string naming the subject identifier variable
in \code{colData(x)}.}

\item{time}{Optional single string naming the time variable in \code{colData(x)}.}

\item{assay}{A single string naming the assay to audit. Defaults to
\code{"counts"}.}

\item{transform}{A single string naming the microbiome transformation to
record for downstream audit modules. Defaults to \code{"clr"}.}

\item{distances}{A character vector naming microbiome distances to record for
downstream audit modules. Defaults to \code{c("aitchison", "bray")}.}

\item{n_perm}{A single positive integer with the planned number of
permutations for downstream audit modules. Defaults to \code{999}.}

\item{verbose}{A single logical value indicating whether future audit modules
should report progress. Defaults to \code{TRUE}.}
}
\value{
A \code{safebiome_audit} object.
}
\description{
\code{check_biome()} is the main entry point for \code{safebiome}. It validates a
\code{SummarizedExperiment}-like object, records the audit parameters, and returns
a stable \code{safebiome_audit} object. Design and correction diagnostics are
evaluated from metadata; batch and leakage diagnostics are represented as
pending placeholders until their modules are implemented.
}
\examples{
data("toy_biome")

audit <- check_biome(
  toy_biome,
  outcome = "outcome",
  batch = "batch"
)

is_biome_audit(audit)
}
