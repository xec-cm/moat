% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-leakage.R
\name{check_leakage}
\alias{check_leakage}
\title{Check validation leakage risk}
\usage{
check_leakage(metadata, outcome, subject = NULL, batch = NULL, time = NULL)
}
\arguments{
\item{metadata}{A data frame with sample metadata.}

\item{outcome}{A single string naming the outcome variable in \code{metadata}.}

\item{subject}{Optional single string naming the subject identifier variable.}

\item{batch}{Optional character vector naming batch variables.}

\item{time}{Optional single string naming the time variable.}
}
\value{
A list with leakage diagnostics, CV recommendations, and a
preprocessing leakage checklist.
}
\description{
\code{check_leakage()} combines repeated-measure, batch-driven, and temporal
validation-leakage diagnostics for ML workflows.
}
\examples{
metadata <- data.frame(
  outcome = rep(c("Control", "Disease"), each = 4),
  patient_id = rep(paste0("P", 1:4), each = 2)
)
check_leakage(metadata, outcome = "outcome", subject = "patient_id")
}
