% Generated by roxygen2: do not edit by hand
\name{report}
\alias{report}
\title{Render a safebiome audit HTML report}
\usage{
report(audit, file = "safebiome_report.html", quiet = TRUE, ...)
}
\arguments{
\item{audit}{A \code{safebiome_audit} object.}

\item{file}{Output HTML file path. Defaults to \code{"safebiome_report.html"}.}

\item{quiet}{A single logical value passed to \code{\link[rmarkdown:render]{rmarkdown::render()}}.
Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to \code{\link[rmarkdown:render]{rmarkdown::render()}}.}
}
\value{
The normalized output file path, invisibly.
}
\description{
\code{report()} renders a self-contained HTML report from a \code{safebiome_audit}
object. The report uses a package-local R Markdown template, so it does not
require internet access while rendering.
}
\examples{
\dontrun{
data("toy_biome")
audit <- check_biome(toy_biome, outcome = "outcome", batch = "batch", n_perm = 99)
report(audit)
}
}
